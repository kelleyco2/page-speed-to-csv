{
  "name": "page-speed-to-csv",
  "version": "1.0.0",
  "description": "Take array of URLs and return the google page speed results in CSV",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Cooper Kelley",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "fast-csv": "^4.3.6",
    "papaparse": "^5.3.2"
  }
}
